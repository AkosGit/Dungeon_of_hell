<UserControl x:Class="Dungeon_of_hell.MultiplayerInGameMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="OrangeRed" Opacity="0.8" />
        </Grid.Background>
        <StackPanel VerticalAlignment="Center">
            <Button Width="100" Height="20" Content="Resume" Command="{Binding Path=Resume}">
            </Button>
            <Button Width="100" Height="20" Content="Settings" Command="{Binding Path=SettingsView}">
            </Button>
            <Button Width="100" Height="20" Content="Exit to main menu" Command="{Binding Path=MainMenuView}">
            </Button>

        </StackPanel>
    </Grid>
  
</UserControl>
