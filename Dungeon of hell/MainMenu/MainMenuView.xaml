<UserControl x:Class="Dungeon_of_hell.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Background="OrangeRed">
        <TextBlock Text="Dungeon of Hell" VerticalAlignment="Center" HorizontalAlignment="Center" 
                   FontWeight="Bold" FontSize="18"
                   />
        <Button Width="100" Height="20" Content="SinglePlayer" Command="{Binding Path=SinglePlayerView}">
        </Button>
        <Button Width="100" Height="20" Content="Multiplayer" Command="{Binding Path=MultiplayerView}" IsEnabled="False">
        </Button>
        <Button Width="100" Height="20" Content="Settings" Command="{Binding Path=SettingsView}">
        </Button>

    </StackPanel>
</UserControl>
